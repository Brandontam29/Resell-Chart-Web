import { css } from 'styled-components';

const general = css`
  a {
    color: inherit;
  }

  h1,
  h2,
  h3,
  h4,
  h5 {
    font-weight: bold;

    a {
      color: inherit;
      text-decoration: none;
    }
  }
`;

export default general;
